import CardPreview from './CardPreview';

const Preview = () => {
      return (
            <div className="h-full">
                  <CardPreview />
            </div>
      );
};

export default Preview;
